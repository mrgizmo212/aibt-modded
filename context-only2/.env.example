# CRITICAL: NO quotes around values! python-dotenv doesn't need them.
# CRITICAL: Use FORWARD slashes in paths (Windows accepts them, avoids escape sequences)

# REQUIRED - AI Model API (OpenRouter recommended for multi-model access)
OPENAI_API_BASE=https://openrouter.ai/api/v1
OPENAI_API_KEY=sk-or-v1-YOUR_OPENROUTER_KEY_HERE

# REQUIRED - Web Search (get free key from jina.ai)
JINA_API_KEY=jina_YOUR_JINA_KEY_HERE

# OPTIONAL - Only needed if fetching new data (data already included)
ALPHAADVANTAGE_API_KEY=YOUR_ALPHAVANTAGE_KEY_HERE

# MCP Service Ports (defaults are fine, don't change unless conflicts)
MATH_HTTP_PORT=8000
SEARCH_HTTP_PORT=8001
TRADE_HTTP_PORT=8002
GETPRICE_HTTP_PORT=8003

# Agent Configuration
AGENT_MAX_STEP=30

# Runtime Environment File - USE FORWARD SLASHES OR RELATIVE PATH!
# Option 1: Absolute path with forward slashes
RUNTIME_ENV_PATH=C:/Users/User/Desktop/CS1027/aitrtader/.runtime_env.json
# Option 2: Relative path (simpler)
# RUNTIME_ENV_PATH=./.runtime_env.json